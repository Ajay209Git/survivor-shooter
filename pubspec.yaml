name: survivor_shooter

description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.0.0
  provider: ^6.0.0
  sqflite: ^2.0.0
  firebase_analytics: ^10.0.0
  google_mobile_ads: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
